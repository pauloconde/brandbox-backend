# MongoDB - Conexión a la base de datos
# Cadena de conexión para MongoDB Atlas. Reemplazar usuario, contraseña, cluster y nombre de base de datos.
MONGODB_URI=mongodb+srv://usuario:contraseña@cluster.mongodb.net/brandbox?retryWrites=true&w=majority&appName=TuCluster

# NextAuth.js - Configuración de autenticación
# URL base de la aplicación, durante desarrollo suele ser localhost:3000
NEXTAUTH_URL=http://localhost:3000
# Valor secreto para firmar tokens y sesiones (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
NEXTAUTH_SECRET=tu_valor_secreto_aleatorio_aqui

# Variables de aplicación
# Entorno actual (development, staging, production)
APP_ENV=development
# Nombre de la aplicación
APP_NAME=BrandBox
# Versión actual de la aplicación
APP_VERSION=0.1.0

# Configuración de tenants
# Idioma predeterminado para nuevos tenants (es, en)
DEFAULT_TENANT_LANGUAGE=es

# Configuración de API (opcional)
# Límite de peticiones por usuario/hora
API_RATE_LIMIT=100
# Tiempo de espera máximo para peticiones en milisegundos
API_TIMEOUT=30000

# Configuración de uploads (opcional)
# Tamaño máximo de archivos en bytes (5MB = 5242880)
UPLOAD_MAX_SIZE=5242880

# Proveedores de autenticación (descomentar y configurar según necesidad)
# Google OAuth
# GOOGLE_CLIENT_ID=tu_client_id_aqui
# GOOGLE_CLIENT_SECRET=tu_client_secret_aqui

# GitHub OAuth
# GITHUB_ID=tu_github_id_aqui
# GITHUB_SECRET=tu_github_secret_aqui

# Email (SMTP para emails transaccionales)
# EMAIL_SERVER=smtp://usuario:contraseña@smtp.example.com:587
# EMAIL_FROM=noreply@example.com